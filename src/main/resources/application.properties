spring.application.name=AuthorizationServer

# Conexión a bd en alojada en aiven
spring.datasource.url=${DATABASE_URL}
spring.datasource.username=${DATABASE_USER}
spring.datasource.password=${DATABASE_PASSWORD}

# Configuración de la base de datos MySQL
# spring.datasource.url=jdbc:mysql://localhost:3306/hospital_h
# spring.datasource.username=root
# spring.datasource.password=

# Configuración de bd en servidor
# spring.datasource.url=jdbc:mysql://100.79.9.108:3306/hospital_h
# spring.datasource.username=userB
# spring.datasource.password=h0spit4L$2026!

spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect

# Configuración de JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# Puerto del servidor 
server.port=9000

# El propio auth server se apunta a sí mismo para decodificar JWT
spring.security.oauth2.resourceserver.jwt.issuer-uri=http://localhost:9000

oauth.client-id=hospital
oauth.client-secret=AJtgST56!dsw34?
oauth.token-uri=http://localhost:9000/oauth2/token
oauth.redirect-uri=http://localhost:4200/callback

logging.level.org.springframework.security=TRACE

# cookieHttp
cookie.secure=false